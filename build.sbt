ThisBuild / version := "0.1.0-SNAPSHOT"

//ThisBuild / scalaVersion := "2.13.10"
ThisBuild / scalaVersion := "10.8.7"

lazy val root = (project in file("."))
  .settings(
    name := "scala_project"
  )
